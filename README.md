# AdvFS-NG

OpenDMI is a cross-platform AdvFS filesystem implementation for Linux / BSD.

## Command line tools

* `advfdmn` - Configures AdvFS domains
* `advfset` - Configures AdvFS filesets
* `advscan` - Locates AdvFS partitions on disks
* `advstat` - Displays file-system statistics
